{{-- Estilos compartidos del formato de OC/OCD (DomPDF). Colores tomados del logo TotalGas. --}}
<style>
    @page { margin: 32px 42px 60px; }
    body { font-family: Helvetica, Arial, sans-serif; color: #27303f; font-size: 8.6px; line-height: 1.42; }
    table { border-collapse: collapse; width: 100%; }
    .head td { vertical-align: middle; }
    .logo { height: 32px; }
    .logo-fallback { color: #034EA2; font-size: 16px; font-weight: bold; }
    .lh { text-align: right; color: #7b8494; font-size: 7.2px; line-height: 1.55; }
    .lh b { color: #034EA2; }
    .hair { border-top: 1px solid #e3e7ee; margin: 12px 0 18px; }
    .title { font-size: 25px; color: #034EA2; letter-spacing: -.3px; }
    .title-folio { font-size: 12px; color: #0095DA; font-weight: bold; letter-spacing: .6px; }
    .kpis { margin-top: 14px; }
    .kpis td { width: 25%; padding: 7px 10px 0 0; vertical-align: top; }
    .kpis td.last { padding-right: 0; }
    .kpi { border-top: 3px solid #034EA2; padding-top: 6px; }
    .kpi-sky { border-top-color: #0095DA; }
    .kpi-green { border-top-color: #009559; }
    .kpi-lime { border-top-color: #A6CE39; }
    .k { color: #8a93a3; font-size: 6.6px; letter-spacing: 1.3px; text-transform: uppercase; }
    .v { font-size: 9.6px; font-weight: bold; color: #111827; margin-top: 2px; }
    .parties { margin-top: 20px; }
    .parties td { width: 50%; vertical-align: top; padding-right: 20px; }
    .parties td.last { padding-right: 0; }
    .party-t { font-size: 7px; font-weight: bold; letter-spacing: 1.3px; text-transform: uppercase; color: #034EA2; margin-bottom: 4px; }
    .party-n { font-size: 10px; font-weight: bold; color: #111827; }
    .sub { color: #6b7483; }
    .items { margin-top: 22px; }
    .items th { color: #8a93a3; font-size: 6.8px; letter-spacing: 1.2px; text-transform: uppercase; font-weight: normal; text-align: left; padding: 0 6px 6px; border-bottom: 1px solid #cfd6e1; }
    .items td { padding: 9px 6px; border-bottom: 1px solid #edf0f5; vertical-align: top; }
    .items tr { page-break-inside: avoid; }
    .idx { color: #0095DA; font-weight: bold; }
    .muted { color: #9aa3b2; font-size: 7px; }
    .note { color: #009559; font-size: 7px; }
    .r { text-align: right; white-space: nowrap; }
    .ctr { text-align: center; }
    .summary { margin-top: 16px; page-break-inside: avoid; }
    .summary > tbody > tr > td { vertical-align: top; }
    .obs-t { font-size: 7px; font-weight: bold; letter-spacing: 1.3px; text-transform: uppercase; color: #009559; margin-bottom: 3px; }
    .obs { color: #58616f; }
    .st td { padding: 3px 0; }
    .total-k { color: #8a93a3; font-size: 7px; letter-spacing: 1.3px; text-transform: uppercase; margin-top: 8px; text-align: right; }
    .total-v { color: #034EA2; font-size: 21px; font-weight: bold; text-align: right; }
    .total-u { border-bottom: 3px solid #A6CE39; width: 70px; margin-left: auto; margin-top: 3px; }
    .signs { margin-top: 46px; page-break-inside: avoid; }
    .signs td { padding: 0 6px; vertical-align: bottom; }
    .sbox { border: 1px solid #e3e7ee; border-radius: 6px; height: 62px; position: relative; }
    .sbox .k { position: absolute; top: 6px; left: 8px; }
    .sname { margin-top: 5px; font-weight: bold; text-align: center; }
    .srole { text-align: center; color: #8a93a3; font-size: 6.8px; text-transform: uppercase; letter-spacing: 1px; }
    .footer { position: fixed; bottom: -44px; left: 0; right: 0; font-size: 6.8px; color: #9aa3b2; }
    .dots td { width: 8px; height: 3px; padding: 0; }
    .dots td.gap { width: 2px; }
</style>
